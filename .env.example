# Copy to `.env.local` (already gitignored) and fill in. Nothing here is read in
# production — the only consumer is the dev-only /api/x-comment route.
#
# Never put this key in a NEXT_PUBLIC_ variable or in a component: this repo
# deploys to flashfx.app, and anything the browser can read is public.
ANTHROPIC_API_KEY=
